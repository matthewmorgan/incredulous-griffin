1. Given two line segments defined by two points each, determine if they intersect.

2. Return the (a) point of intersection

`[[x0, y0], [x1, y1]]` is the definition of a segment, where all values are integers.